import React, {
  Dispatch,
  SetStateAction,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useState,
} from 'react';
import { useGetInfoProfileQuery } from '../../../store/services';



const useUpdateProfileModels = () => {

  const values=useMemo(() => {return {}},[]);

  return values;
};
export default useUpdateProfileModels;
